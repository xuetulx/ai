## 8. 临时文件与缓存规则

### 8.1 临时文件管理

1. `[DO]` 所有临时文件统一放入 `tmp/`、`.temp/` 或 `.cache/` 目录
2. `[DO]` 临时文件命名建议加时间戳或任务 ID，避免冲突
3. `[DO]` 任务完成后自动清理临时文件（除非用户要求保留）
4. `[DO]` 长时间运行的任务，定期清理过期临时文件（> 24 小时）

### 8.2 缓存文件管理

1. `[DO]` 缓存文件放入 `.cache/` 目录，按模块分子目录
2. `[DO]` 缓存文件命名包含版本标识，便于失效清理
3. `[DO]` 读取缓存前验证有效性，过期则重新生成
4. `[DO]` 不得将敏感信息存入缓存文件
