# dsh-plugin-rules bundle patch: mounts the rules plugin row.
# The row id must match the plugin's `export const name` in lib/index.js.
- insert:
    - id: plugin-rules
      name: 'dsh-plugin-rules'
